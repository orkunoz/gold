import{readFileSync}from"node:fs";import{describe,expect,it}from"vitest";const read=(p:string)=>readFileSync(new URL(p,import.meta.url),"utf8");describe("focused UI cleanup",()=>{it("uses the exact owner inventory order and excludes its transfer source",()=>{const s=read("./inventory-bulk-table.tsx");expect(s).toContain('["number","productCategory","article","producer","size","weight",...(canManage?["purchasePrice"]:[]),"pricePerGram","priceUah","status","shop","barcode"]');expect(s).toContain("sources.length!==1||s.id!==sources[0]")});it("keeps checkout discount while removing sale notes and cart fineness",()=>{const checkout=read("./sales-checkout.tsx");expect(checkout).toContain("discountPercent");expect(checkout).not.toContain("sales.saleNotes");expect(checkout).not.toContain("item.gold_fineness");expect(checkout).not.toContain("notes:");});it("removes only the general Inventory search field",()=>{const filters=read("./inventory-filters.tsx");expect(filters).not.toContain('name="search"');for(const name of ["barcode","article","category","status","shop"])expect(filters).toContain(`name="${name}"`);expect(filters).toContain("INVENTORY_FILTER_DEBOUNCE_MS")});it("interpolates the modal count and styles its destructive action",()=>{const table=read("./inventory-bulk-table.tsx");expect(table).toContain('{count:selected.length}');expect(table).toContain('inventory.bulk.${mode}Description');expect(table).toContain('bg-red-700');expect(table).toContain('inventory.deletePermanently')})});
