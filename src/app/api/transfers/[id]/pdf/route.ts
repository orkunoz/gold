import{getTransfer}from"@/lib/transfers/queries";import{transferPdf}from"@/lib/transfers/pdf";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,{transfer,items}=await getTransfer(id),pdf=transferPdf(transfer,items);return new Response(new Uint8Array(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="${transfer.transfer_number}.pdf"`,"Cache-Control":"private, no-store"}})}
