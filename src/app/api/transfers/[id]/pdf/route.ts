import{getTransfer}from"@/lib/transfers/queries";import{transferPdf,transferPdfFilename}from"@/lib/transfers/pdf";export const runtime="nodejs";export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,{transfer,items}=await getTransfer(id),pdf=transferPdf(transfer,items);return new Response(new Uint8Array(pdf),{headers:{"Content-Type":"application/pdf","Content-Disposition":`attachment; filename="${transferPdfFilename(transfer.transfer_number)}"`,"Content-Length":String(pdf.byteLength),"Cache-Control":"private, no-store"}})}
