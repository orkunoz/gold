import {EmployeeForm} from "@/components/employee-form"; import {inviteEmployee} from "@/lib/admin/actions"; import {getAdminData} from "@/lib/admin/queries"; export default async function InviteEmployeePage(){const{shops}=await getAdminData();return <section><h1 className="text-3xl font-semibold">Invite employee</h1><p className="mt-2 text-sm text-stone-600">The invitation is sent through Supabase Auth from the server. Retrying the same email is safe.</p><div className="mt-8"><EmployeeForm action={inviteEmployee} shops={shops.filter(x=>x.is_active)} invite/></div></section>}
