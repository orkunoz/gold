import {EmployeeForm} from "@/components/employee-form"; import {createEmployeeAccount} from "@/lib/admin/actions"; import {getAdminData} from "@/lib/admin/queries"; export default async function CreateEmployeePage(){const{shops}=await getAdminData();return <section><h1 className="text-3xl font-semibold">Create account</h1><div className="mt-6"><EmployeeForm action={createEmployeeAccount} shops={shops.filter(x=>x.is_active)} invite/></div></section>}
