import {notFound} from "next/navigation"; import {ShopForm} from "@/components/shop-form"; import {updateShop} from "@/lib/admin/actions"; import {getAdminShopRecords} from "@/lib/admin/queries"; export default async function EditShopPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const shops=await getAdminShopRecords();const shop=shops.find(x=>x.id===id);if(!shop)notFound();return <section><h1 className="text-3xl font-semibold">Edit shop</h1><div className="mt-8"><ShopForm action={updateShop.bind(null,id)} shop={shop}/></div></section>}
