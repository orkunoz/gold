import Link from"next/link";import{getTransfers}from"@/lib/transfers/queries";import{getTranslations}from"@/lib/i18n/server";import{formatPrice}from"@/lib/inventory/format";
export default async function TransfersPage(){const[{t,locale},rows]=await Promise.all([getTranslations(),getTransfers()]);return <section><h1 className="text-3xl font-semibold">{t("transfers.title")}</h1><div className="mt-6 overflow-x-auto rounded-xl border bg-white"><table className="w-full text-left text-sm"><thead className="border-b bg-stone-50"><tr>{["number","date","from","to","items","totalWeight","totalValue"].map(k=><th key={k} className="px-4 py-3">{k==="date"?t("fields.date"):t(`transfers.${k}`)}</th>)}</tr></thead><tbody>{rows.map(x=><tr key={x.id} className="border-b"><td className="px-4 py-3"><Link className="underline" href={`/transfers/${x.id}`}>{x.transfer_number}</Link></td><td className="px-4 py-3">{new Intl.DateTimeFormat(locale==="ua"?"uk-UA":"en-US",{dateStyle:"medium",timeStyle:"short"}).format(new Date(x.transferred_at))}</td><td className="px-4 py-3">{x.source_name}</td><td className="px-4 py-3">{x.destination_name}</td><td className="px-4 py-3">{x.item_count}</td><td className="px-4 py-3">{x.total_weight} g</td><td className="px-4 py-3">{formatPrice(x.total_value,locale)}</td></tr>)}</tbody></table></div></section>}
