import {describe,expect,it} from "vitest"; import {employeeAssignmentValid,normalizeShopCode,validEmail} from "./validation";
describe("admin validation",()=>{it("normalizes shop codes",()=>expect(normalizeShopCode(" kyiv-1 ")).toBe("KYIV-1"));it("validates emails",()=>{expect(validEmail("staff@example.com")).toBe(true);expect(validEmail("bad email")).toBe(false);});it("requires shops for active operational roles",()=>{expect(employeeAssignmentValid("manager","",true)).toBe(false);expect(employeeAssignmentValid("salesperson","shop",true)).toBe(true);expect(employeeAssignmentValid("owner","",true)).toBe(true);});});
