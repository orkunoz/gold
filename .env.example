NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Canonical HTTPS application URL, without a trailing slash. Used for Auth invitation redirects.
NEXT_PUBLIC_APP_URL=
# Server-only. Required only for Owner employee invitation; never prefix with NEXT_PUBLIC_.
SUPABASE_SERVICE_ROLE_KEY=
